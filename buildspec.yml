version: 0.2
phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
  build:
    commands:
      - pip install -r requirements.txt
  post_build:
    commands:
      - echo "Post-build phase"
artifacts:
  files:
    - '**/*'
